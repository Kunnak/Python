def get_zahlen(liste):
    for item in liste:
        if type(item) == int:
            liste_neu.append(item)

    print(liste_neu)

liste = ['a', 452, 'g', 546, 'p', 'c',785, 'r', 't', 68, 'd', 648]
liste_neu = []

get_zahlen(liste)
