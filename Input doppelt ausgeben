def verdoppeln(string):
    for buchstabe in string:
        liste.append(buchstabe)
        liste.append(buchstabe)

string = input('String: ')
liste = []

verdoppeln(string)

ausgabe = ''.join(liste)
print(ausgabe)
